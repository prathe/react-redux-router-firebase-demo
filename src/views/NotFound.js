import React from 'react';

// Our client-side "Not Found" page outside HTTP scope.
// which should never be rendered unless we mess up.
const NotFound = () => {
  return (
    <h1>Not Found.</h1>
  );
};

export default NotFound;
